[package]
name = "rsbts-scale-benchmark"
version = "0.0.0"
publish = false
edition = "2021"

[dependencies]
rsbts = { path = ".." }
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json = "1"
tempfile = "3"
